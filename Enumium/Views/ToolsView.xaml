<UserControl x:Class="Enumium.Views.ToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:Enumium.Helpers">

    <UserControl.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="28,24,28,0">
            <TextBlock Text="Advanced Tools" Style="{StaticResource PageTitleStyle}"/>
            <TextBlock Text="System utilities and diagnostic tools" Style="{StaticResource BodyTextStyle}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Tool Tabs -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="28,16,28,0">
            <RadioButton Style="{StaticResource TabButtonStyle}" Content="System Info" IsChecked="True"
                         GroupName="ToolTabs" Command="{Binding SelectToolCommand}" CommandParameter="0"/>
            <RadioButton Style="{StaticResource TabButtonStyle}" Content="Processes" Margin="4,0"
                         GroupName="ToolTabs" Command="{Binding SelectToolCommand}" CommandParameter="1"/>
            <RadioButton Style="{StaticResource TabButtonStyle}" Content="Benchmark" Margin="4,0"
                         GroupName="ToolTabs" Command="{Binding SelectToolCommand}" CommandParameter="2"/>
        </StackPanel>

        <!-- Tool Content -->
        <Grid Grid.Row="2" Margin="28,16,28,20">
            <!-- System Info (Tool 0) -->
            <Border Padding="20">
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource CardStyle}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedTool}" Value="1">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding SelectedTool}" Value="2">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding SystemInfo}" FontFamily="{StaticResource MonoFont}"
                               FontSize="12" Foreground="{StaticResource TextPrimaryBrush}"
                               TextWrapping="Wrap" LineHeight="20"/>
                </ScrollViewer>
            </Border>

            <!-- Process Manager (Tool 1) -->
            <Grid>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedTool}" Value="1">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Button Content="Refresh" Style="{StaticResource SecondaryButtonStyle}"
                        HorizontalAlignment="Right" Margin="0,0,0,8"
                        Command="{Binding RefreshProcessesCommand}"/>

                <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="16">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!-- Header -->
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="70"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="PID" Style="{StaticResource SmallTextStyle}" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="Name" Style="{StaticResource SmallTextStyle}" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="2" Text="Memory" Style="{StaticResource SmallTextStyle}" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="3" Text="Status" Style="{StaticResource SmallTextStyle}" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="4" Text="Action" Style="{StaticResource SmallTextStyle}" FontWeight="SemiBold"/>
                            </Grid>
                            <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,0,0,4"/>

                            <ItemsControl ItemsSource="{Binding Processes}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,3">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="60"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="70"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Pid}"
                                                       FontFamily="{StaticResource MonoFont}" FontSize="11"
                                                       Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="12"
                                                       Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="2" FontSize="11" VerticalAlignment="Center"
                                                       Foreground="{StaticResource SecondaryAccentBrush}"
                                                       FontFamily="{StaticResource MonoFont}">
                                                <TextBlock.Text>
                                                    <Binding Path="MemoryMB" StringFormat="{}{0} MB"/>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <TextBlock Grid.Column="3" Text="{Binding Status}" FontSize="11"
                                                       Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                            <Button Grid.Column="4" Content="End" Style="{StaticResource DangerButtonStyle}"
                                                    Height="24" MinWidth="44" FontSize="10"
                                                    Command="{Binding DataContext.KillProcessCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding Pid}"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>

            <!-- Benchmark (Tool 2) -->
            <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedTool}" Value="2">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>

                <Button Content="Run Benchmark" Style="{StaticResource PrimaryButtonStyle}"
                        HorizontalAlignment="Left" Margin="0,0,0,16" MinWidth="160"
                        Command="{Binding RunBenchmarkCommand}"/>

                <Border Style="{StaticResource CardStyle}" Padding="24" MinHeight="300">
                    <TextBlock Text="{Binding BenchmarkResult}" FontFamily="{StaticResource MonoFont}"
                               FontSize="14" Foreground="{StaticResource TextPrimaryBrush}"
                               TextWrapping="Wrap" LineHeight="24"
                               VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
